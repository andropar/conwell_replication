# Default min_nn evaluation config. Override on the CLI.

features:    features/
splits_root: resources/splits/
out:         results/min_nn/
voxel_set:   hlvis
alphas:      [0.1, 1.0, 10.0, 100.0, 1000.0, 10000.0, 100000.0]

subjects:
  - sub-01
  - sub-03
  - sub-05
  - sub-06
  - sub-07

# Optional: restrict to a subset of splits
# splits:
#   - random_0
#   - tau_balanced_adaptive_stochastic
