# Archived Split-Half Results

This directory preserves the previous cached-layer odd/even split-half outputs.

- Source commit: `833117c` (`Add results`)
- Raw result parquet directory: `/ptmp/rothj/conwell_replication/eval_splithalf_otc_ncsnr`
- Layer coverage: cached approximately ten-layer second-half subset per model
- Summary: 590 result files, 711,564 score rows, 117 models

The current full second-half streamed split-half outputs live in
`figures/splithalf_results`.
